#!/bin/sh
xargs brew install < brew.installed


# Don't forget to install rescuetime, flux, chrome, vscode (run vscodesettings.sh after install), and ohmyzsh?, iterm2, install new version of vim (maybe also create alias vim to point to new version)

# when installing iterm2:
# on iterm2, go to profile, settings, colors, put solarized dark, change background to black, and set font as 'Menlo 14pt in iTerm 2'
